# Default host port is 9003 (9000–9999 range; not 9001 or 9090).
# To use another port in that range, change the left side only, e.g. "9003:8080".
services:
  veil:
    build: .
    ports:
      - "9005:8080"
    volumes:
      - veil-data:/data
    restart: unless-stopped

volumes:
  veil-data:
